NEXT_PUBLIC_API_BASE_URL=http://localhost:8080/v1

# Local override only — never commit a real value here. The deployed backend's URL lives in
# the git-tracked .env.production instead, since it's public config, not a secret.
